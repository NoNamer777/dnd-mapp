import { CommonThemeVariables } from './definition';

export const common: CommonThemeVariables = {
    '--fw-regular': 400,
    '--fw-bold': 600,
    '--icon-size': '1rem',
    '--border-radius': '0.4rem',
    '--opacity-disabled': 0.333,
};
